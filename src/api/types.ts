export interface User {
    username: string;
    email: string;
};

export interface Post {
    id: number;
    username: string;
    title: string;
    text: string;
    createdAt: string;
    editedAt: string;
};
